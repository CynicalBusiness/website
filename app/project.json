{
    "name": "app",
    "$schema": "../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "app/src",
    "projectType": "application",
    "tags": [],
    "targets": {
        "stylelint": {
            "executor": "nx-stylelint:lint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["app/**/*.css"]
            }
        }
    }
}
